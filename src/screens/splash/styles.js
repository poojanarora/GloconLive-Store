import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    safeAreaViewStyle:{
        flex: 1,
    },
    splashImageStyle: {
        flex: 1,
        justifyContent: "center"
    }
});

export default styles;